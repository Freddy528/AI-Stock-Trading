# Claude 协作指令

## 项目信息
- 项目名称：AI-Stock-Trading（AI炒股赚大钱）
- GitHub 地址：https://github.com/Freddy528/AI-Stock-Trading
- 用户说"push到github" = push 到上述仓库

## 交易系统参数
- 初始资金：¥10,000（模拟账户）
- 风格：激进型
- 最大持仓：2只
- 单股仓位上限：60%
- 允许空仓
- 每日最多4次交易（买卖各算1次）
- 选股范围：沪市主板(60开头) + 深市主板(000/001开头)，排除ST/科创/创业板

## 每日工作流程
- **09:25 盘前报告**：市场分析 + 给出明确的买入推荐（股票+挂单价+仓位）
- **用户确认后**：视为已按该价格挂单
- **14:50 尾盘报告**：收盘前操作建议（如有尾盘买入机会）
- **盘中**：用户随时可问，给出实时分析
- **收盘后**：每日复盘 + 模拟盘结算 + 更新 memory 文件

## 模拟盘自动结算规则（重要！）
1. 盘前给出推荐，用户确认 = 挂单生效
2. 收盘后检查当日K线：如果当日最低价 ≤ 挂买价，则视为**成交**，以挂单价买入
3. 如果当日最低价 > 挂买价，则视为**未成交**
4. 卖出同理：如果当日最高价 ≥ 挂卖价，则视为成交
5. 成交后调用 `src/account.py` 的 buy/sell 函数更新模拟账户
6. 每日收盘后调用 snapshot 记录当日净值
7. 更新 watchlist.md 的后续追踪表

## 策略一致性规则（重要！）
- **`memory/strategies.md` 是唯一策略源**。`src/signal_generator.py` 会在每次生成信号时动态读取该文件
- **任何策略变更都必须更新 `memory/strategies.md`**，不要只在聊天中口头调整。改完后策略会自动生效到下一次信号输出
- **禁止在代码中硬编码策略规则**。止盈/止损/选股/仓位等规则全部写在 `strategies.md` 中
- 策略变更后，在 `strategies.md` 底部的"策略迭代日志"表格中追加记录

## 工作规范
- 每次重要改动后更新 `memory/strategies.md` 记录策略变更
- 踩坑或重要发现记录到 `memory/lessons.md`
- 重大更新记录到 `docs/changelog.md`
- **每次交互都要更新** `docs/从0到1搭建AI炒股系统.md`，总结为教学步骤
- 代码放在 `src/` 目录下
- 数据文件放在 `data/` 目录下（注意 .gitignore 排除大文件）
- 提交信息使用中文，简洁明了

## 每日复盘模板（收盘后必做）
复盘文件：`docs/daily/YYYY-MM-DD.md`，内容包含：
1. **大盘概况**：指数涨跌、涨跌家数、涨停/跌停数、情绪判断
2. **涨得好的票**：
   - 连板龙头排行（连板数、行业、点评）
   - 板块主线分析（哪些板块涨停最多、逻辑是什么）
   - 放量上涨但未涨停的好形态票
3. **昨日涨停今日表现**：溢价率、上涨比例、连板率
4. **今日教训与发现**：市场规律、风格切换、资金动向
5. **明日关注方向**：延续哪些主线、新出现的方向
6. **持仓操作记录**：当日买卖、账户净值

### 复盘→策略回写（重要！）
复盘完成后，必须检查是否有需要沉淀到策略的内容：
- 发现新的有效规律/形态 → 更新 `memory/strategies.md` 对应章节
- 发现某条策略规则不适用 → 修改或标注失效原因
- 操作失误暴露策略漏洞 → 补充风险管理规则
- 更新后在 `strategies.md` 底部"策略迭代日志"追加记录
- **不回写 = 白复盘**，复盘的价值在于让下一次信号输出变得更好

## 交易决策输出格式
每次给出建议时包含：
1. 市场概况（大盘/情绪/资金）
2. 当前持仓分析
3. 具体操作建议（买入/卖出/持有）
4. 挂单价格（买入价区间 / 卖出价区间）
5. 决策依据（技术面 + 消息面）
6. 风险提示
